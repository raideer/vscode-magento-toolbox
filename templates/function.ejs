<% if (description || returnType || docParams.length > 0) { -%>
/**<% if (description) {%>
  * <%= description %>
  *<% } %>
<% for (const i in docParams) { -%>
  * @param <%= docParams[i].type %> $<%= docParams[i].name %>
<% } -%>
<% if (docParams.length > 0) {-%>
  *
<% } -%>
<% if (returnType) {-%>  * @return <%= returnType %><% } -%>
  */
<% } -%>
<% if (params.length > 0) {-%>
<%= visibility %> function <%= name -%>(
<% for (const i in params) { -%>
    <% if (params[i].type) {-%><% if (params[i].nullable == true) {-%><%= `?` %><% } -%><%= `${params[i].type} ` %><% } -%>$<%= params[i].name %><% if (params[i].value) {-%><%- ` = ${params[i].value}` %><% } -%><% if (i < params.length - 1) {%>,<% } %>
<% } -%>
) {
<% } else { -%>
<%= visibility %> function <%= name -%>()
{
<% } -%>
<%- data %>
}