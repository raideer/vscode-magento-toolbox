/** <% if (description) {%>
  * <%= description %>
  * <% } %>
<% for (const i in docParams) { -%>
  * @param <%= docParams[i].type %> $<%= docParams[i].name %>
<% } -%>
<% if (docParams.length > 0) {-%>
  *
<% } -%>
  * @return <%= returnType %>
  */
<% if (params.length > 0) {-%>
<%= visibility %> function <%= name -%>(
<% for (const i in params) { -%>
    <%= params[i].type %> $<%= params[i].name %><% if (i < params.length - 1) {%>,<% } %>
<% } -%>
) {
<% } else { -%>
<%= visibility %> function <%= name -%>()
{
<% } -%>
<%- data %>
}